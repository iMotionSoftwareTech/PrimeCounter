# PrimeCounter
The PrimeCounter program takes an integer n as a command-line argument and calculates the number of prime numbers less than or equal to n, specifically noting the need for an efficient algorithm for large inputs like 10 million.
